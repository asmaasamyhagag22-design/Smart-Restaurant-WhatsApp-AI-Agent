# ────────────────────────────────────────────────────────────
#  Smart Restaurant WhatsApp AI Agent — runtime dependencies
#  Local-first: everything here runs without any paid API.
# ────────────────────────────────────────────────────────────

# ---- API gateway ----
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29
gunicorn>=21.2
python-multipart>=0.0.9
jinja2>=3.1

# ---- Config / models ----
pydantic>=2.6,<3.0
pydantic-settings>=2.2

# ---- LLM / Agent orchestration ----
anthropic>=0.40
langgraph>=0.2
langchain-core>=0.3

# ---- Persistence ----
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
alembic>=1.13
pgvector>=0.2.5
redis>=5.0

# ---- Embeddings (local, ONNX — no torch) ----
fastembed>=0.3
numpy>=1.26

# ---- Task queue ----
celery[redis]>=5.3

# ---- HTTP clients / resilience ----
httpx>=0.27
tenacity>=8.2

# ---- Observability ----
structlog>=24.1
prometheus-client>=0.20

# ---- Utils ----
python-dotenv>=1.0
